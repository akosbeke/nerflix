import { screen } from '@testing-library/react'

import { render } from '../../test/providers'
import NumberedRow from '../NumberedRow'

test('NumberedRow shows the top 10 titles in Hungary', () => {})
